clear
clc

N = 100;
for i = 1:N
    
    if i <= 20
        disp(num2str(i)) % this is the easier command possible
    elseif i <=50
        
    else
        disp(num2str(i)) % this is the easier command possible
    end
    
end